supabase>=2.4
httpx>=0.27
curl_cffi>=0.7        # giả TLS fingerprint trình duyệt, lách chống bot
openai>=1.30          # dùng làm client chung cho OpenRouter / NVIDIA NIM (OpenAI-compatible)
pydantic>=2.7
pydantic-settings>=2.2
tenacity>=8.3
python-dotenv>=1.0
opencc-python-reimplemented>=0.1.7   # phồn → giản thể cho yêu cầu truyện nhập tên tiếng Việt
