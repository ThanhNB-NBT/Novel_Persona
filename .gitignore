# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Environment variables
.env
.env.local
.env.production

# IDE / AI agents
.vscode/
.idea/
.claude/
.agents/
.codex/
.codex-remote-attachments/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
/logs/
*.log

# Test coverage
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Docker
docker-compose.override.yml
DOCKER_COMMANDS.md

# Node (frontend)
node_modules/
.next/
out/
.nuxt/

# Build
*.pyc
*.pyo

.codegraph/

# so tay VPS chua IP/ha tang — repo public, khong commit
worker/VPS.md
worker/vps.bat
worker/vps.ps1

# ket qua eval dich (noi dung chuong) va benchmark model — ket luan ghi vao docs, khong commit artifact
worker/eval_out*/
worker/benchmark_out/
worker/corpus_translation/

# Caches nang (HuggingFace hub, uv) — khong bao gio commit
.hf-cache*/
.uv-cache*/

# Qoder IDE agent
.qoder/

# Thu muc tam cua pytest (--basetemp). Vai cai cu do Docker tao, ACL khoa,
# xoa phai takeown — ignore de git khoi canh bao moi lan chay.
.pytest-g*/
.tmp-pytest-*/
**/.tmp-pytest-*/

# Goi model tai ve / artifact nen (nhi phan lon)
*.zip

# Data goc / corpus finetune — de o may, khong commit vao repo.
# Hub hachimi/: theo doi script + tai lieu + bao cao, bo qua data va goi zip.
worker/hachimi/data/
worker/hachimi/packs/
worker/hachimi/**/*.jsonl
worker/hachimi/**/*.bak*

# Model CT2 Hachimi cho worker (nhi phan, khong commit)
worker/models/
worker/hachimi/models/

.tmp_eval_g3/
skills-lock.json

# Goi Kaggle nen (binary lon)
worker/hachimi/packs/**/*.zip

# backup glossary do script dọn sinh ra (dữ liệu người dùng, không lên repo public)
worker/glossary_unanchored_backup_*.json
